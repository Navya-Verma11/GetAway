// SPDX-License-Identifier: MIT
pragma solidity ^0.8.0;

contract PaymentContract {
    function processPayment() external payable {
        // Process payment and update booking status
        // Ensure security and error handling for payments
    }
    
    function refundPayment(address _recipient, uint256 _amount) external {
        // Refund payment to the recipient
        // Ensure security and error handling for refunds
    }
}
